package test.repository.order;

import com.repository.order.OrderRepositoryImpl;

public class OrderRepositoryImplTest {

	   public static void main(String[] args) {
		     
		 /* Order order = new Order();
		  
	      order.setMemberId((long)1);
	      order.setDeliveryId((long)10001);
	      order.setStatusId((long)6);
	      order.setQuantity(2);
	      order.setPrice((long)2000);
	      order.setFinalPrice((long) 2000);
	      */
	      
	      OrderRepositoryImpl or = new OrderRepositoryImpl();
	      // 주문
	      // or.createOrder(order);
	      
	      /*
	      // 주문 취소
	      long num = 7;
	      or.cancelOrder(num);
	      */
	      
	      /*
	      // 주문 상태 변경
	      long orderId = 7;
	      String statusName = "배송완료";
	      or.confirmOrder(orderId, statusName);
	  	  */
	   
	   }
}
